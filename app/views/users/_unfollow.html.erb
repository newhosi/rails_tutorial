<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), method: :delete) do |f| %>
  <%= f.submit "Unfollow", class: "rounded-md bg-gray-300  hover:bg-gray-400 px-2 py-1 text-white font-bold text-base w-full"%>
<% end %>
