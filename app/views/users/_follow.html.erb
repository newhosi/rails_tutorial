<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <%= hidden_field_tag :followed_id, user.id %>
  <%= f.submit "Follow", class: "rounded-md bg-blue-300  hover:bg-blue-400 px-2 py-1 text-white font-bold text-base w-full" %>
<% end %>
